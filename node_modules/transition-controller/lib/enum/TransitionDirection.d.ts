/**
 * A transition can go into predefined directions, this enum defines those directions.
 */
declare enum TransitionDirection {
    /**
     * The IN direction of the transition.
     */
    IN = 0,
    /**
     * The OUT direction of the transition.
     */
    OUT = 1,
}
export default TransitionDirection;
